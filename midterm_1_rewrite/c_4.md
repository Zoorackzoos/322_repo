# Section C: Lambda Calculus (35 points)
Problem 4 (15 points)  
for each expression  
* List all free variables  
* List all bound variables  
* Rewrite using α-conversions so no variable is bound more than once

```
I write my Lambda calculus more verbosely than others. 
I didn’t write this section as nicely as the question asked for. 
So this will be a pure rewrite.
I will start with the α-conversion and then the free & bound variables.
```

```text
(a) (3 pts) λx. ( λy. x y ) ( λx. y x )
λx. (  ( λy. ( x y ) ) ( λx. ( y x )  ) )
->
λx. (  ( λy1. ( x y1 ) ) ( λx1. ( y x1 )  ) )
free variables = y
bound variables = x, x1, y1

(b) (3 pts) ( λx. λy x ( λx. y x ) ) ( λy. y z)
( λx. ( λy. ( x ( λx. ( y x ) ) ) ) )
```
